#!/bin/sh
set -euf
top="$(git rev-parse --show-toplevel)"
"$top/bin/topics" |
while read topic; do
    dir="$top/topics/$topic/"
    if [ -e "$dir" ] && [ -f "$dir/index.md" ] && [ ! -f "$dir/tutorial-by-claude-opus-4-5.md" ]; then
        echo "$topic"
    fi
done
